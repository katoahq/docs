<Tabs>
   <Tab title="yarn">
    
    ```bash
    yarn global add @cicadahq/cicada
    ```

  </Tab>
  <Tab title="npm">
    
    ```bash
    npm install -g @cicadahq/cicada 
    ```
  </Tab>
  <Tab title="pnpm">
    
    ```bash
    pnpm install -g @cicadahq/cicada 
    ```
  </Tab>
  {/*
  </Tab>
  <Tab title="brew">
    ```bash
    brew install cicada
    ```
  </Tab> */}
  <Tab title="curl">
    ```bash
    curl -fSsL https://cicada.build/install | sh
    ```
  You must open a new shell for the CLI to work
  </Tab>
</Tabs>

<em>
  Note: Cicada requires [Docker](https://docs.docker.com/engine/install/) &
  [deno](https://deno.land/manual@v1.32.3/getting_started/installation)
</em>
